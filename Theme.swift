import SwiftUI

extension Color {
    static let primaryBlue = Color(red: 0.35, green: 0.45, blue: 0.95)
    static let accentBlue = Color(red: 0.55, green: 0.65, blue: 1.0)
    static let successGreen = Color(red: 0.15, green: 0.80, blue: 0.45)

    static let appBackground = Color(red: 0.96, green: 0.97, blue: 1.0)
    static let cardBackground = Color.white
}
